<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusAssistant">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LostandFoundActivity"
            android:exported="false"
            android:label="@string/title_activity_lostand_found"
            android:theme="@style/Theme.CampusAssistant"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".IdleHallActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusAssistant" />
        <activity
            android:name=".ui.LostandFoundAddActivity"
            android:exported="false" />
        <activity android:name=".ui.TaskDetailActivity" android:exported="false"/>
        <activity
            android:name=".ui.AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusAssistant" />
        <activity android:name=".PublishIdleActivity" />
        <activity android:name=".PublishBuyActivity" />
        <activity android:name=".PublishCarpoolActivity" />
        <activity android:name=".PublishErrandActivity" />
        <activity android:name=".PublishLostActivity" />
        <activity
            android:name=".ForumActivity"
            android:exported="false"
            android:label="@string/title_activity_forum"
            android:theme="@style/Theme.CampusAssistant"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
